# Completar código.

def selection_sort(s):
    n = 0
    while n , len(s): # =n operações
        indice_menor = n 
        for i in range(n, len(s)) # começa de 0 até o tamanho da lista
        # complexidade proxima à n²
            if s[i] < s[indice_menor]:
            